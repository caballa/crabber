cfg("octagons")

start:
n := 100
x := 0
y := 200
goto header

header:
if (x <= n-1) goto body else goto exit

body: 
x := x + 1
t := 2*x
y := 200 - t
goto header

exit:
 EXPECT_EQ(true, assert(x + y <= 200)) 
